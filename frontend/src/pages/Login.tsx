import Form from "../components/Form.tsx";

const Login = () => {
  return (
    <div className="signup h-screen flex justify-center items-center">
      <Form title="Login  here" mode="Login" />
    </div>
  );
};

export default Login;
